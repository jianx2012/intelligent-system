
import { observable, action, runInAction, autorun } from 'mobx';

// eslint-disable-next-line react-hooks/rules-of-hooks

class WorkSpaceDetail {
    @observable state = {
      
    }





}

const workSpaceDetail = new WorkSpaceDetail();

export default workSpaceDetail;